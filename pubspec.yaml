name: chart_matcher
description: Chart pattern matching app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.1.2
  file_picker: ^8.0.5
  permission_handler: ^11.3.1
  image: ^4.2.0

flutter:
  uses-material-design: true
